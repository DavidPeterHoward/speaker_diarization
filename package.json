{
  "name": "audiotranscribe-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for AudioTranscribe application",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.30.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
