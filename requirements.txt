# Core dependencies
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
numpy==1.26.4
soundfile==0.12.1
scipy==1.11.3
librosa==0.10.1
python-json-logger==2.0.7
dataclasses-json==0.6.1

## Optional heavy backends are intentionally commented out for portability in CI/testing.
## The application will automatically fall back to mock backends when these are absent.
# openai-whisper==20231117
# faster-whisper==1.1.1
# transformers>=4.26.0

# pyannote.audio==3.1.1
# resemblyzer==0.1.2
# webrtcvad==2.0.10

# Testing
pytest>=7.4.0

# PyTorch (optional)
# torch>=2.0.0
# torchaudio>=2.0.0
# For CUDA support, install from: https://pytorch.org/get-started/locally/

# Background job queue (optional but recommended for production)
# Requires Redis server to be running
redis>=5.0.0
rq>=1.15.0

# Machine learning utilities for optimized diarization (optional)
# scikit-learn>=1.3.0  # For KMeans clustering in speaker diarization

# Optional dependencies
# av==10.0.0  # For video processing if needed
# tensorflow==2.14.0  # For TensorFlow-based models
# onnxruntime==1.16.0  # For ONNX model inference